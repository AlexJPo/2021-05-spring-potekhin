spring:
  datasource:
    url: jdbc:h2:mem:bookdb
    username: user
    password: book123
    driver-class-name: org.h2.Driver
    initialization-mode: always
    data: test_data.sql

  shell:
    interactive:
      enabled: false